server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Thiruop/my-config-repo.git
          clone-on-start: true
          default-label: main
